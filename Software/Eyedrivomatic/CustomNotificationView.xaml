<UserControl x:Class="Eyedrivomatic.CustomNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:dwellClick="clr-namespace:Eyedrivomatic.Eyegaze.DwellClick;assembly=Eyedrivomatic.Eyegaze"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Height="350" Width="550">

    <DockPanel x:Name="LayoutRoot" Background="{StaticResource WindowBackgroundBrush}">
        <Label Content="{Binding Title}" DockPanel.Dock="Top" HorizontalAlignment="Center" FontWeight="Bold" FontSize="28"/>
        <Button DockPanel.Dock="Bottom" 
                Content="{Binding ButtonContent}" 
                Height="50" Width="400" Margin="5" Click="Button_Click"
                dwellClick:DwellClickBehavior.IgnorePause="True"/>
        <Viewbox Margin="5">
            <TextBlock Width="470" TextWrapping="Wrap" Text="{Binding Content}" FontWeight="Bold" TextAlignment="Left"/>
        </Viewbox>
    </DockPanel>
</UserControl>
